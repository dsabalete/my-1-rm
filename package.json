{
  "name": "my-1-rm",
  "version": "1.6.0",
  "private": true,
  "scripts": {
    "clean:all": "bun run cleanup && rm -rf node_modules && rm bun.lock",
    "cleanup": "nuxt cleanup",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "release": "semantic-release",
    "docker:build": "docker build -t dsabalete/my-1-rm .",
    "docker:run": "docker run -p 3000:3000 dsabalete/my-1-rm",
    "docker:push": "docker push dsabalete/my-1-rm"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.11.3",
    "nuxt": "^4.0.0",
    "pinia": "^3.0.4",
    "usebootstrap": "^3.5.107",
    "vue": "^3.5.22"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^11.0.1",
    "semantic-release": "^24.2.0"
  }
}
