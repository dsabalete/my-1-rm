{
  "name": "my-1-rm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean:all": "bun run cleanup && rm -rf node_modules && rm bun.lockb && bun i",
    "cleanup": "nuxt cleanup",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.9.0",
    "nuxt": "latest",
    "pinia": "^2.3.0",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "@nuxt/types": "^2.18.1"
  }
}