# Copy this file to .env and fill in your analytics IDs

# Note: Google Tag Manager Container ID is required for tracking to work
# Analytics will only be active in production mode (NODE_ENV=production)
# GA4 should be configured externally through your GTM container
#
# Note: For contact form to work, configure Resend API key and email addresses
# The contact form will work without email service in development (logs to console)
# but requires Resend configuration for production

# Google Tag Manager Container ID
# Format: GTM-XXXXXXX
# Get your ID from: https://tagmanager.google.com/
NUXT_PUBLIC_GOOGLE_TAG_MANAGER_ID=<YOUR_GOOGLE_TAG_MANAGER_CONTAINER_ID>

# Site URL for SEO (sitemap, canonical URLs, etc.)
# Format: https://yourdomain.com
NUXT_PUBLIC_SITE_URL=https://1rm.davidsabalete.com

# Email Service Configuration (for contact form)
# Resend API Key (FREE: 3,000 emails/month)
# Get your API key from: https://resend.com/api-keys
# Sign up at: https://resend.com (free tier available)
# Format: re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
RESEND_API_KEY=<YOUR_RESEND_API_KEY>

# Email address to send contact form emails from
# This should be a verified domain in your Resend account
# For testing, you can use: onboarding@resend.dev (only works for testing)
# For production, use: noreply@yourdomain.com (requires domain verification)
# Format: noreply@yourdomain.com or onboarding@resend.dev (testing only)
RESEND_FROM_EMAIL=<YOUR_RESEND_FROM_EMAIL>

# Email address to receive contact form submissions
# Format: info@yourdomain.com
CONTACT_RECIPIENT_EMAIL=<YOUR_CONTACT_RECIPIENT_EMAIL>

# Note: At least one analytics ID is required for tracking to work
# Analytics will only be active in production mode (NODE_ENV=production)
#
# Alternative free email services:
# - Brevo (formerly Sendinblue): 300 emails/day free
# - Mailgun: 100 emails/day free (first 3 months)
# - Mailjet: 200 emails/day free
PROD=true
